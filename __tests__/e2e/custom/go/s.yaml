edition: 3.0.0
name: framework
access: quanxi

resources:
  fcDemo:
    component: ${env('fc_component_version', path('../../../../'))}
    actions: 
      pre-deploy:
        - run: go mod tidy
          path: ./code
        - run: GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o target/main main.go
          path: ./code
    props:
      region: cn-hangzhou
      function:
        functionName: fc3-event-custom-go
        description: 'hello world by serverless devs'
        timeout: 30
        memorySize: 512
        runtime: custom
        code: ./code/target
        customRuntimeConfig:
          command:
            - '/code/main'
